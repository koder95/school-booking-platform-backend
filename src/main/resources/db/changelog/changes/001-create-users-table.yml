databaseChangeLog:
  - changeSet:
      id: 001-create-users-table
      author: Koder95
      changes:
        - createTable:
            tableName: users
            columns:
              - column:
                  name: uuid
                  type: uuid
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: DTYPE
                  type: varchar(250)
                  constraints:
                    nullable: false
              - column:
                  name: email_id
                  type: bigint
                  constraints:
                    unique: true
                    nullable: false
                    foreignKeyName: fk_users_email_id
                    references: emails(id)
              - column:
                  name: password_hash
                  type: varchar(128)
              - column:
                  name: authority
                  type: varchar(16)
                  constraints:
                    nullable: false
              - column:
                  name: zone_id
                  type: varchar(64)
                  constraints:
                    nullable: false
              - column:
                  name: is_trial
                  type: boolean
                  defaultValueBoolean: false
                  constraints:
                    nullable: false
              - column:
                  name: is_deleted
                  type: boolean
                  defaultValueBoolean: false
                  constraints:
                    nullable: false
